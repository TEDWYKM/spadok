{
  "name": "spadok",
  "version": "0.11.0",
  "private": true,
  "description": "Спадок — застосунок історико-культурного туризму. 15 історико-географічних регіонів, наповнено Галичину і Наддніпрянщину.",
  "license": "MIT",
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "icons": "node scripts/icons.mjs",
    "serve": "node scripts/serve.mjs",
    "release": "node scripts/release.mjs",
    "check": "node scripts/check.mjs",
    "e2e": "node scripts/e2e.mjs",
    "sync": "cap sync android",
    "apk": "cap sync android && cd android && ./gradlew assembleDebug",
    "aab": "cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/geolocation": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "leaflet": "^1.9.4"
  }
}
