# Спадок — застосунок історико-культурного туризму

Продуктова специфікація MVP. Версія 2, 17.08.2026.

---

## 1. Що це

Мобільний застосунок, який популяризує українську історію та культуру
через туризм. Карта пам'яток → тематичні маршрути → навігація → контент,
що відкривається на місці → нагороди за пройдені маршрути.

**Ключова механіка:** контент про пам'ятку і можливість її оцінити
розблоковуються тільки після фізичного прибуття в точку. Це відрізняє
продукт від путівника і захищає рейтинги від накрутки.

**Регіон MVP:** Львівська область, 17 точок.
Причина вибору — найвища концентрація замків, готовий впізнаваний маршрут
«Золота підкова», відносно безпечний регіон із живим туристичним потоком.

---

## 2. Обсяг MVP

### Увійшло
- Інтерактивна карта (Leaflet + OpenStreetMap), два стилі підкладки
- 17 точок з описом, статусом доступності, рейтингом
- 6 тематичних фільтрів
- Сортування: найпопулярніші / найближчі
- 9 маршрутів трьох розмірів: короткі (1 день), середні (2 дні), великі (3–4 дні)
- Прокладання по реальних дорогах через OSRM, зі страховкою на пряму лінію
- Наскрізний сценарій подорожі з розбивкою по днях
- Картка пам'ятки: фото / реконструкція, короткий і повний опис
- **Оцінка 1–5 зірок + текстовий відгук, тільки з місця**
- Штампи відвідин з датою і часом
- 10 нагород (з 50 запланованих)
- Банер повітряної тривоги, статуси точок, кнопка «повідомити про проблему»

### Свідомо не увійшло
- Платний аудіо/відео-шар — тільки місце в інтерфейсі
- Реальна GPS-геолокація і геофенсинг — замінено кнопкою «Я на місці»
- Реєстрація, синхронізація між пристроями
- Стрічка відгуків інших користувачів
- Бронювання житла, партнерські інтеграції

---

## 3. Джерела даних

| Тип | Джерело | Статус |
|---|---|---|
| Пам'ятки | Держреєстр нерухомих пам'яток (e-pamiatka.gov.ua) | треба домовитись про доступ |
| Пам'ятки | Відкриті дані data.gov.ua | вільне використання |
| Пам'ятки | Краудсорсинг з модерацією | треба побудувати процес |
| Карта | OpenStreetMap | безкоштовно, обов'язкова атрибуція |
| Маршрутизація | OSRM (демо-сервер) | для продакшену — власний інстанс |
| Тривоги | api.ukrainealarm.com або alerts.in.ua | потрібен токен за заявкою |
| Фото | Музеї, заповідники, фотографи | потрібні договори |
| Реконструкції | Історики + ілюстратори / 3D | замовна робота, найдорожча стаття |

**Найбільший ризик проєкту — не код, а контент.** Реконструкції й описи
для сотень точок неможливо зробити силами малої команди без партнерств.

---

## 4. Модель даних

```
Point
  id, name, lat, lon
  themes[]              одна точка може мати кілька тем
  kind                  castle | ruin | sacral | city | rock
  status                ok | warn | closed | occupied
  status_updated_at     дата останньої перевірки — критичне поле
  summary, full_text
  media[]               photo | reconstruction | audio | video
  paid_media[]          що доступно тільки за підпискою
  nearby                shops, lodging, food
  caution               попередження для мандрівника
  rating_avg, rating_count

Route
  id, name, size        s | m | l
  days[][]              масив днів, кожен день — масив point_id
  base_point_id         точка старту й повернення
  computed              km, minutes (рахуються, не зберігаються)

Visit
  user_id, point_id, visited_at, verified_by  gps | manual

Rating
  user_id, point_id, stars 1..5, text, created_at
  visit_id              обов'язковий — без відвідин оцінка не приймається

Award
  id, name, condition, unlocked_at
```

---

## 5. Правила, які не можна порушувати

1. **Оцінка тільки після підтвердженої відвідини.** Rating без Visit
   не створюється на рівні бази, не тільки в інтерфейсі.
2. **Статус точки має дату перевірки.** Точка без свіжої перевірки
   не потрапляє в «рекомендовані».
3. **Маршрут не прокладається через території з непідтвердженим статусом.**
4. **Офлайн обов'язковий.** Багато точок у зоні слабкого зв'язку.
5. **Атрибуція OpenStreetMap на кожній карті.** Вимога ліцензії.
6. **Трек користувача — персональні дані.** Мінімізація зберігання,
   прозора політика, шифрування.

---

## 6. Наступні кроки

### Найближчі
1. Зібрати дані по одній області з відкритих джерел і Держреєстру
2. Замовити реконструкції для 5–7 якірних точок — вони продають платну версію
3. Винести перевірку статусів у регулярний процес з відповідальною людиною
4. Підключити реальний геофенсинг і перевірити на 3–4 точках у полі

### Далі
5. Платний шар: аудіо для якірних точок
6. Стрічка відгуків, фото від користувачів
7. Розширення на Тернопільщину і Хмельниччину (Кам'янець, Меджибіж)
8. Тема «Гетьманщина» — з'явиться разом з Центральною Україною
9. Партнерства із закладами на маршрутах
10. B2G-модуль для обласних управлінь туризму

---

## 7. Відкриті питання

- Власний OSRM-інстанс чи платний маршрутизатор?
- Хто модерує краудсорсинг на старті?
- Ціна підписки й що саме за неї отримує користувач?
- Чи потрібна реєстрація в MVP, чи прогрес живе локально?
- Юридична модель прав на фото: викуп, ліцензія, чи атрибуція?

---

## 8. Файли

Прототипи, з яких почався продукт, лежать в `legacy/`:

- `legacy/spadok-mvp-v2.html` — прототип, жива карта Leaflet
- `legacy/spadok-mvp-v1.html` — перша версія, схематична карта

Робочий застосунок — у `www/`. Що саме змінилося при переході від
прототипу до застосунку, і які правила з розділу 5 тепер справді
виконуються в коді, описано в `README.md`.
